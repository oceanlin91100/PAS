@model UniPsg.Model.PAS.ViewModels.OrganizationImportModel
@{
    ViewBag.Title = "考核人員匯入";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<h5><i class="fa fa-cog" aria-hidden="true"> &nbsp;設定管理</i>  &nbsp;&gt;&nbsp; <a href="@Url.Action("Index","AssessProject")">考核專案</a>  &nbsp;&gt;&nbsp; 考核人員匯入 </h5>
<center><h4>@ViewBag.Title</h4></center>
<div class="form-horizontal">
    <hr />
    @using (Html.BeginForm("Imports", "AssessPerson", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @*<div class="form-group">
            @Html.LabelFor(m => m.ProjectId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.ProjectId, new { @class = "form-control", @Value = ViewBag.ProjectId, @readonly = "readonly" })
                @Html.ValidationMessageFor(m => m.ProjectId, "", new { @class = "text-danger" })
            </div>
        </div>*@
        <div class="form-group">
            @Html.LabelFor(m => m.ProjectName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.ProjectName, new { @class = "form-control", @Value = ViewBag.ProjectName, @readonly = "readonly" })
                @Html.ValidationMessageFor(m => m.ProjectName, "", new { @class = "text-danger" })
            </div>
        </div>       
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a href="~/AssessProject" class="btn btn-warning">不儲存退出</a>
                <input type="submit" value="匯入" class="btn btn-success" />
                @Html.HiddenFor(m => m.Creator, new { @Value = @User.Identity.Name })
                @Html.HiddenFor(m => m.ProjectId)
            </div>
        </div>
    }
</div>



